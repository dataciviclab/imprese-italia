[project]
name = "imprese-italia"
version = "0.1.0"
description = "Imprese Italia — demografia d'impresa italiana da Camera di Commercio delle Marche"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["ruff", "pytest"]

[tool.setuptools]
py-modules = []

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
