root: "../../out"
schema_version: 1

dataset:
  name: "camcom_stock_italia_6cifre"
  source_id: "camcom-marche"
  tags: [imprese, economia, ateco, ateco-6cifre, camcom, micro]
  category: economia
  years: [2026]
  time_coverage:
    start_year: 2026
    end_year: 2026

raw:
  output_policy: overwrite
  sources:
    - name: "download"
      type: "script"
      args:
        command: "python3 camcom_download.py raw_input.csv"
        output: "raw_input.csv"
        filename: "raw_input.csv"
      primary: true

clean:
  sql: "sql/clean.sql"
  read:
    source: auto
    mode: latest
    delim: ";"
    encoding: utf-8
    header: true
  required_columns:
    - regione
    - provincia
    - settore
    - imprese
  validate:
    min_rows: 1000
    not_null: [regione, settore]

mart:
  tables:
    - name: "mart_6cifre_regione"
      sql: "sql/mart_6cifre_regione.sql"
    - name: "mart_6cifre_provincia"
      sql: "sql/mart_6cifre_provincia.sql"
  required_tables: ["mart_6cifre_regione", "mart_6cifre_provincia"]
  validate:
    table_rules:
      mart_6cifre_regione:
        required_columns: [regione, settore, imprese]
        min_rows: 100
      mart_6cifre_provincia:
        required_columns: [provincia, settore, imprese]
        min_rows: 100
